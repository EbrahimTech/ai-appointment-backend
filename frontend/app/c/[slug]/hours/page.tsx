"use client";

import ServicesPage from "../services/page";

/**
 * Reuse the services admin page for the standalone /hours route.
 * Both URLs exist because some navigation items point directly to /hours.
 */
export default ServicesPage;
